package org.virtuslab.bazelsteward.maven

import org.virtuslab.bazelsteward.core.Workspace

class MavenDependencyExtractor {
    fun extract(workspace: Workspace): List<MavenCoordinates> {
        return emptyList()
    }
}