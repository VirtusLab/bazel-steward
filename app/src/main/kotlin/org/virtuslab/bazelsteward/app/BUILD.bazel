kt_jvm_library(
    name = "app",
    srcs = glob(["**/*.kt"]),
    visibility = ["//visibility:public"],
    deps = [
        "//bazel",
        "//core",
        "//maven",
    ],
)

lint(srcs = glob(["**/*.kt"]))
